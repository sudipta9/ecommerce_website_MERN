import React from "react";
import Layout from "../../components/Layouts";

const Orders = (props) => {
  return (
    <Layout sidebar>
      Orders
      <span>dhv</span>
    </Layout>
  );
};

export default Orders;
